export interface ExtenstionListItem {
  id: AnyListen.Extension.Extension['id']
  name: AnyListen.Extension.Extension['name']
}

// export type ExtensionSettingItem = NonNullable<AnyListen.Extension.Extension['contributes']['settings']>[number]
